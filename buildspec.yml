version: 0.2
phases:
  install:
    commands:

#      - echo testing stuff
#      - aws-iam-authenticator verify -t k8s-aws-v1.really_long_token -i cluster-name

      - echo Entered the install phase...
      - apt-get update -y
#      - apt-get install -y maven

      - curl -o aws-iam-authenticator https://amazon-eks.s3-us-west-2.amazonaws.com/1.12.7/2019-03-27/bin/linux/amd64/aws-iam-authenticator
      - chmod +x ./aws-iam-authenticator
      - mkdir -p ~/bin && cp ./aws-iam-authenticator ~/bin/aws-iam-authenticator && export PATH=~/bin:$PATH
      - curl -LO https://storage.googleapis.com/kubernetes-release/release/$(curl -s https://storage.googleapis.com/kubernetes-release/release/stable.txt)/bin/linux/amd64/kubectl
      - chmod +x kubectl
      - mv ./kubectl /usr/local/bin/kubectl
      - wget https://storage.googleapis.com/kubernetes-helm/helm-v2.14.0-linux-amd64.tar.gz -O helm.tar.gz; tar -xzf helm.tar.gz
      - chmod +x ./linux-amd64/helm
      - mv ./linux-amd64/helm /usr/local/bin/helm


    finally:
      - echo This always runs even if the update or install command fails 
  pre_build:
    commands:
      - echo Entered the pre_build phase...

      - echo Logging in to Amazon ECR...
      - aws --version
      - $(aws ecr get-login --region eu-central-1 --no-include-email)

      - mkdir ~/.kube/
      - cp config.yml ~/.kube/config


    finally:
      - echo This always runs even if the login command fails 
  build:
    commands:
      - echo Entered the build phase...
      - echo Build started on `date`
#      - mvn install
#      - mvn package
    finally:
      - echo This always runs even if the install command fails
  post_build:
    commands:
      - echo Entered the post_build phase...

      - ls -a ~

#      - unset AWS_ACCESS_KEY_ID
#      - unset AWS_SECRET_ACCESS_KEY
#      - unset AWS_SESSION_TOKEN

      - aws ecr get-login-password --region eu-central-1 | docker login --username AWS  --password-stdin 378582414855.dkr.ecr.eu-central-1.amazonaws.com

#      - docker build -t aws-test .

#      - docker tag aws-test:latest 378582414855.dkr.ecr.eu-central-1.amazonaws.com/new-ecr:latest

#      - docker push 378582414855.dkr.ecr.eu-central-1.amazonaws.com/new-ecr:latest

      - kubectl config get-contexts

#      - aws eks update-kubeconfig --name viper-eks-cluster --region eu-central-1 --role-arn arn:aws:iam::378582414855:role/New-Iteratec-Codebuild-Project

      #      - aws sts assume-role --role-arn "arn:aws:iam::378582414855:role/New-Iteratec-Codebuild-Project" --role-session-name "Test_CodeBuild_Session"
      - CREDENTIALS=$(aws sts assume-role --role-arn arn:aws:iam::378582414855:role/viper-eks-role --role-session-name New-Iteratec-Codebuild-Project --duration-seconds 900)
      - export AWS_ACCESS_KEY_ID="$(echo ${CREDENTIALS} | jq -r '.Credentials.AccessKeyId')"
      - export AWS_SECRET_ACCESS_KEY="$(echo ${CREDENTIALS} | jq -r '.Credentials.SecretAccessKey')"
      - export AWS_SESSION_TOKEN="$(echo ${CREDENTIALS} | jq -r '.Credentials.SessionToken')"
      - export AWS_EXPIRATION=$(echo ${CREDENTIALS} | jq -r '.Credentials.Expiration')
      -
      - aws eks update-kubeconfig --name viper-eks-cluster --region eu-central-1 --role-arn arn:aws:iam::378582414855:role/viper-eks-role
#      - aws eks update-kubeconfig --name viper-eks-cluster

      - aws sts get-caller-identity

      - kubectl get nodes

      #      - aws eks describe-cluster --region eu-central-1 --name viper-eks-cluster --query cluster.certificateAuthority.data
      - kubectl apply -f aws-auth-cm.yml
      #      - kubectl get secrets
      #      - kubectl get secret $SECRET_NAME -o json | jq -r '.data["token"]' | base64 -D



      - kubectl get svc

#        - aws --region eu-central-1 eks update-kubeconfig --name viper-eks-cluster --role-arn arn:aws:iam::378582414855:role/service-role/New-Iteratec-Codebuild-Project
      #      - aws --region eu-central-1 eks update-kubeconfig --name eks-cluster --role-arn arn:aws:iam::378582414855:role/service-role/Iteratec-Test-MK


      #      - kubectl get svc


      -  kubectl config get-contexts
      - kubectl get nodes


      #      - kubectl get -n kube-system configmap/aws-auth -o yaml | awk "/mapRoles: \|/{print;print \"$ROLE\";next}1" > /tmp/aws-auth-patch.yml

      #      - kubectl patch configmap/aws-auth -n kube-system --patch "$(cat /tmp/aws-auth-patch.yml)"

#      - cd helm && helm install awstest --set image.repository=378582414855.dkr.ecr.eu-central-1.amazonaws.com/aws_ecr_test --set image.tag=latest .

      - echo Build completed on `date`



#  aws ecr get-login-password --region eu-central-1 | docker login --username AWS  --password-stdin 378582414855.dkr.ecr.eu-central-1.amazonaws.com
